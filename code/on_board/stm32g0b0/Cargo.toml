[package]
name = "stm32g0b0"
version = "0.1.0"
edition = "2021"

# hint that this is a library
# because contents of src/ are generated by the build script
[lib]
path = "src/lib.rs"

[features]
atomics = []
critical-section = ["dep:critical-section"]
rt = []

[dependencies]
cortex-m = { version = "0.7" }
critical-section = { version = "1.1", optional = true }
vcell = { version = "0.1" }
